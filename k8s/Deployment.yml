apiVersion: apps/v1
kind: Deployment
metadata:
  name: java-war-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: java-war-app
  template:
    metadata:
      labels:
        app: java-war-app
    spec:
      containers:
      - name: java-container
        image: ranigali/java-war-app:v1
        ports:
        - containerPort: 8080
